#pragma once

#include "app\app.h"
#include <vector>
#include "Path.h"

static class Palette {
public:
	//map
	std::vector<Path> map;

	void InitMap(int level) {
		switch (level)
		{
		case 1:


		default:
			break;
		}
	
	}


	//draws player sprite in position
	void DrawMap() {
		
	}

};

